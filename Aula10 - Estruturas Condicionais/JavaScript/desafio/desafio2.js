let distancia = 400;
let distanciaMaxima = 200;
valor = 0;

if (distancia <= distanciaMaxima){
    valor = distancia * 0.5
    console.log("O valor da viagem foi de " + valor + " R$")

} else{
    valor = distancia * 0.45
    console.log("O valor da viagem foi de " + valor + " R$")

}